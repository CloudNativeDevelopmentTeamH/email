apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.email.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.email.serviceAccount.roleArn }}
