apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.email.pullSecret.name }}
  namespace: {{ .Values.namespace | quote  }}
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: {{ .Values.email.pullSecret.dockerconfigjson | quote }}
